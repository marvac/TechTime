@using TechTime.ViewModels
@model JobEntryViewModel
@{
    ViewBag.Title = "Create Work Entry";
}
@section Styles{

}
@section Scripts{

}

<div class="row">

    <form method="post" class="col-lg-6 col-lg-offset-3">

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">@ViewBag.Error</div>
        }
        <div class="form-group">
            <label asp-for="CustomerId"></label>
            <select class="form-control" asp-for="CustomerId" asp-items="@(new SelectList(Model.CustomerList, "CustomerId", "Name"))"></select>
        </div>
        <div class="form-group">
            <label asp-for="Type"></label>
            <select class="form-control" asp-for="Type" asp-items="@(new SelectList(Model.JobTypes, "Description", "Description"))"></select>
        </div>
        <div class="form-group">
            <label asp-for="Hours"></label>
            <span class="text-danger" asp-validation-for="Hours"></span>
            <input type="number" step="0.25" asp-for="Hours" class="form-control" />
        </div>

        <div class="form-group">
            <label asp-for="ContactName"></label>
            <input type="text" asp-for="ContactName" class="form-control" />
        </div>

        <div class="form-group">
            <label asp-for="Description"></label>
            <span class="text-danger" asp-validation-for="Description"></span>
            <textarea textarea asp-for="Description" class="form-control" rows="5"></textarea>
        </div>

        <div>
            <input type="submit" value="Submit" class="btn btn-primary" />
        </div>

    </form>
</div>